<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode. -->
    <style name="Base.Theme.LeafiIHC" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand colors. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryLight</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryContainer">@color/colorAccentLight</item>
        
        <!-- Background colors. -->
        <item name="android:colorBackground">@color/colorBackgroundDark</item>
        <item name="colorSurface">@color/colorSurfaceDark</item>
        <item name="colorSurfaceVariant">@color/colorCardBackgroundDark</item>
        
        <!-- Text colors. -->
        <item name="android:textColorPrimary">@color/colorTextPrimaryDark</item>
        <item name="android:textColorSecondary">@color/colorTextSecondaryDark</item>
        <item name="android:textColorHint">@color/colorTextHintDark</item>
        
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Card style. -->
        <item name="materialCardViewStyle">@style/Widget.App.CardView.Dark</item>
        
        <!-- Button style. -->
        <item name="materialButtonStyle">@style/Widget.App.Button.Dark</item>
    </style>

    <!-- Card style for dark mode. -->
    <style name="Widget.App.CardView.Dark" parent="Widget.MaterialComponents.CardView">
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="cardBackgroundColor">@color/colorCardBackgroundDark</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Button style for dark mode. -->
    <style name="Widget.App.Button.Dark" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">8dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Theme.LeafiIHC" parent="Base.Theme.LeafiIHC" />
</resources>